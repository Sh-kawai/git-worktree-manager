# Local Codex configuration for the worktree container directory.

default_permissions = "worktree-container"
approval_policy = "on-request"

[permissions.worktree-container.filesystem]
":minimal" = "read"
":tmpdir" = "write"
":slash_tmp" = "write"
glob_scan_max_depth = 8

[permissions.worktree-container.filesystem.":workspace_roots"]
"." = "write"
"main" = "deny"
"develop" = "deny"
"**/.env" = "deny"
"**/.env.local" = "deny"
"**/.env.*.local" = "deny"
"**/.env.1password" = "deny"
"**/*.pem" = "deny"
"**/*.key" = "deny"
"**/*_rsa" = "deny"
"**/*_ed25519" = "deny"

[permissions.worktree-container.network]
enabled = false

[shell_environment_policy]
inherit = "core"
ignore_default_excludes = false
exclude = [
  "*KEY*",
  "*SECRET*",
  "*TOKEN*",
  "*PASSWORD*",
  "ANTHROPIC_API_KEY",
  "BACKLOG_API_KEY",
  "BRIDGE_SECRET",
  "CODEX_API_KEY",
  "OPENAI_API_KEY",
  "ORACLE_*",
]
