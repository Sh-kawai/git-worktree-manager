# Codex 作業ルール

このリポジトリは、複数の Git worktree を `git-worktree/` 配下に置くための親リポジトリです。

このリポジトリ自体は、`git-worktree/` 内の各プロジェクトの仕様、業務ルール、実装詳細を管理対象にしません。

## 作業対象

- このリポジトリで扱うのは、worktree 配置を支える管理ファイルとスクリプトに限定する。
- `git-worktree/` 配下の中身は、原則としてこのリポジトリの作業対象外とする。
- `_shared/common/` と `_shared/worktrees/` 配下の実データは、原則としてこのリポジトリの作業対象外とする。
- 各 worktree 内の `CLAUDE.md`、`AGENTS.md`、README、実装ファイルは、その worktree 側のルールとして扱い、この親リポジトリの判断材料にしない。

## worktree 内でのパス記載

- `git-worktree/`配下の各リポジトリは、そのworktreeのルートより上位にあるディレクトリ、ファイル、親リポジトリの構成を認識することを前提にしない。
- worktree内のドキュメント、コード、コメント、設定例へパスを記載する場合は、そのworktreeから見えるパスを使用し、worktree内で完結させる。
- `../`等でworktreeのルート外へ移動する相対パス、worktree外のローカル絶対パス、親リポジトリ固有のディレクトリ名を、worktree内のファイルへ記載しない。
- worktree内のシンボリックリンクから参照できる場合は、リンク先の物理パスではなく、worktree内のシンボリックリンクを起点とするパスを記載する。例えば`data/`から参照できるファイルは`data/...`と記載する。
- worktree内から参照できるパスがなく、worktree外のファイルへ言及する必要がある場合は、外部の配置を示すパスを書かず、原則としてファイル名だけを記載する。
- このルールはworktree内の成果物に対する記載方法を定めるものであり、親リポジトリ自身の管理設定や配置説明には適用しない。

## Git 管理

- このリポジトリでは、管理用ファイルだけを Git に含める。
- `git-worktree/`、`_shared/common/`、`_shared/worktrees/`、`docs/` の中身は `.gitkeep` を除き追跡しない。
- `.worktree-shared.toml` はローカル設定として扱い、Git には含めない。
- 共有用のひな形は `.worktree-shared.sample.toml` として管理する。

## 編集前承認

- ファイル編集の前に、必ず変更方針を提示する。
- ユーザーの明示的な承認を得るまで、ファイル作成・編集・削除を行わない。
- 承認前に許可されるのは、ファイル構成確認、検索、読み取り、Git 状態確認のみ。

## シークレット

- `.env`、`.env.*`、秘密鍵ファイルは読まない。
- API キー、DB パスワード、トークン、社内認証情報を出力しない。

## コードコメント

- 新規追加・変更するコードコメントは日本語で書く。

## Git 操作

- ユーザーの未コミット変更を勝手に戻さない。
- `git reset --hard`、`git checkout --`、`rm` などの破壊的操作は、ユーザーの明示承認なしに実行しない。
- GitHub へ共有する変更は、原則として `git-worktree/` 配下の `feat/*` ブランチ worktree で作成する。

## サブエージェント

複雑なタスクでは、必要に応じてサブエージェントを利用する。

目的は、メインエージェントのコンテキスト消費と高コストモデルの使用量を抑えつつ、調査の網羅性と実装精度を維持することである。

### サブエージェントに委任する作業

以下のような、範囲が明確で読み取り中心の作業は、可能な限り低コストなサブエージェントへ委任する。

* リポジトリやディレクトリ構成の探索
* 関連ファイル・関数・クラス・シンボルの特定
* コードの実行経路の調査
* 類似実装や既存パターンの検索
* テスト構成の調査
* テストの実行と失敗原因の整理
* ログやエラーメッセージの解析
* 修正による影響範囲の調査
* 実装後の初期レビュー

ただし、この親リポジトリで作業する場合、サブエージェントも `git-worktree/`、`_shared/common/`、`_shared/worktrees/` 配下の中身を原則として探索対象にしない。

ユーザーが特定の worktree を明示的に作業対象として指定した場合は、その worktree を独立したリポジトリとして扱い、その worktree 内の `AGENTS.md` などのルールに従う。

### メインエージェントが担当する作業

メインエージェントは主に以下を担当する。

* 問題の整理とタスク分解
* 根本原因の判断
* 設計・アーキテクチャ上の判断
* 実装方針の決定
* 重要なコード修正
* サブエージェントの調査結果の統合
* サブエージェント間で結果が食い違った場合の判断
* 最終的な検証

精度が求められる設計判断、根本原因の判断、重要な実装判断を、低コストなサブエージェントだけで確定しない。

### サブエージェント利用時のルール

* サブエージェントには、明確で限定された作業を与える。
* 独立して調査できる作業は、必要に応じて並列で実行する。
* 同じ内容を複数のサブエージェントに不必要に重複して調査させない。
* サブエージェントがすでに調査した内容を、メインエージェントが不必要に再調査しない。
* 小さく単純な修正では、無理にサブエージェントを利用しない。
* 調査やテストなど、読み取り中心の作業を優先してサブエージェントに任せる。
* 複数のサブエージェントによる同一コードへの同時編集は避ける。
* 書き込みを伴う作業をサブエージェントへ委任する場合も、「編集前承認」のルールを継承する。ユーザーの承認前にファイルを変更してはならない。
* サブエージェントにも、この `AGENTS.md` に定められた作業範囲、シークレット、Git 操作などの制約を適用する。

### サブエージェントからの結果返却

サブエージェントは、ファイル全文や長いログをメインエージェントへ返さず、判断に必要な情報だけを簡潔に要約する。

原則として、以下の内容を返す。

1. 結論
2. 根拠となるファイル・関数・クラス・シンボル
3. 確認した内容
4. 不明点または追加確認が必要な点

* 推測よりも、実際のコード、テスト、ログから確認できた根拠を優先する。
* 必要に応じてファイルパスやシンボル名を示すが、不要なコード全文は返さない。
* 長いテスト出力やログはそのまま返さず、エラー箇所と判断に必要な部分だけを要約する。

### コストを意識したモデル利用

* サブエージェントには、原則としてタスクを十分に遂行できる範囲で低コストなモデルを使用する。
* コード探索、ファイル検索、テスト調査、ログ解析、影響範囲調査、初期レビューでは、低コストなサブエージェントを優先する。
* 難しい調査や低コストモデルでは十分な確度が得られない場合のみ、より高性能なモデルへ切り替える。
* 単純な作業に高い reasoning effort を常用しない。
* サブエージェントの数を増やすこと自体を目的にしない。独立した作業が存在し、並列化によるメリットがある場合にのみ追加する。
- メインエージェントのコンテキストへ大量の探索結果を持ち込まず、サブエージェント側で情報を絞り込んでから返す。
